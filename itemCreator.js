
const Contenedor = require("./Contenedor")

const itemContainer = new Contenedor();


//Grabo los objetos en el archivo / contenedoras
//itemContainer.save({ title: `Harina`, price: 78, picture: `foto de harina` });
//itemContainer.save({ title: `Huevos`, price: 112, picture: `foto de maple` });
// itemContainer.save({ title: `Arroz`, price: 140, picture: `foto de arroz` });

//busco un item por id:
//itemContainer.getById(1);


//todos los items del objeto:
//itemContainer.getAll();

//Elimino item por id
//itemContainer.deleteById(2)

//Elimina todos los items
//itemContainer.deleteAll()
