import assert from 'mocha';
import request from ('supertest')(app);
import { app } from './main.js';

import { expect } from 'chai';

describe('Test para los metodos de autenticacion', ()=> {
    it('Esto retorna una instancia de la clase', () => {

    })
})


describe('Test para los metodos de carrito', ()=> {
    it('Esto retorna una instancia de la clase', () => {

    })
})

describe('Test para los metodos de productos', ()=> {
    it('Esto retorna una instancia de la clase', () => {

    })
})
